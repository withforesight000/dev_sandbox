#!/usr/bin/env bash
set -euo pipefail

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)

command -v python3 >/dev/null 2>&1 || {
  echo "devcontainer allowlist: python3 is required on the host" >&2
  exit 1
}

exec python3 "$script_dir/prepare-mounts.py" "$@"
